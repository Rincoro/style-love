<div class="row">
  <div class="col-12">
      <% if @articles.present? %>
        <h2 class="text-2xl font-bold text-gray-800 ">みんなの推し活コーデ</h2>
        <div class="grid gap-x-4 gap-y-6 sm:grid-cols-2 md:gap-x-6 lg:grid-cols-4 xl:grid-cols-4">
          <%= render @articles %> 
        </div>
        </div>
      <% else %>
        <p><%= t('articles.index.no_articles') %></p>
      <% end %>
    </div>
  </div>
</div>